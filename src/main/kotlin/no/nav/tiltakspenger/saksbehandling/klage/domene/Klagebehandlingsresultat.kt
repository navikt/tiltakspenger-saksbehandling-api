package no.nav.tiltakspenger.saksbehandling.klage.domene

enum class Klagebehandlingsresultat {
    /** Merk at en avvisning ikke er det samme som et avslag. Det er et vedtak som kan klages på. */
    AVVIST,
}
