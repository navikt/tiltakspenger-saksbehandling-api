package no.nav.tiltakspenger.saksbehandling.benk.v2.domene

/**
 * Fanene i benk v2.
 * Én fane er én spørring med sitt eget radformat, sitt eget filter og sitt eget sett med sorteringskolonner.
 */
enum class BenkV2Fane {
    SØKNADER,
    REVURDERINGER,
    MELDEKORT,
    KLAGE,
    TILBAKEKREVING,
}
